
/*
HINT

import React from 'react';

import ConfessionList from '../components/ConfessionList';

import connectToStore from './connectToStore.jsx';

class ConfessionListContainer extends React.Component {
	render() {
		const {
			confessions
		} = this.props;

		return (
			<ConfessionList confessions={confessions} />
		);
	}
}

export default connectToStore(ConfessionListContainer);

*/
